# Size-field composition

Compose multiple per-point edge-length contributions into a single
`SizeFieldFn` suitable for passing to `triangulate(size_field=...)`.

## SizeFieldFn

::: admesh.SizeFieldFn

## compose_size_field

::: admesh.compose_size_field
