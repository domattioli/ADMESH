<!-- Session introspection + corpus entry. Caveman ultra. -->
---
date: 2026-06-13
session: 2026-06-13T07Z-rotation
repo: domattioli/ADMESH
severity: low
freq: recurring
issues: [155, 286, 150, 48]
wasted_min: 5
wasted_tok: 12000
missing_skill: null
---

# ADMESH rotation 2026-06-13T07Z — maintenance slot

## Summary

Hour-07 → ADMESH (3rd A slot today: 01Z PR#153/probe, 04Z octree PR#156, now 07Z). Overhaul A-slice operator-gated/shipped → maintenance track. Shipped 2 fixes on `development` → rolling PR#150: (1) **#155** `inpaint_nans` 1-D Laplacian column misalignment — locked stage-13, fix mirrors correct 2-D branch, moves toward MATLAB fidelity; (2) **dead drift-gate** — startup hook false-passed while pin behind when `sync-from-domi` absent/403 (hit at own bootstrap) → ported Valence #147 offline sibling-clone fallback + smoke. Hub loop: filed **DomI#286** (canonicalize the now-divergent per-repo drift-gate copies — on-mission for #48). Suite 439 pass / 14 skip / 1 xfail. 3 Haiku dispatches (2 inpaint passes + 1 hook), Fable 5 orchestrate/review.

## What worked (top 3, evidence)

1. **Revert-check caught a false regression test.** Haiku's first #155 test passed even with the bug stashed (linear ramp ∈ operator null space). Stash-and-rerun before commit exposed it → replaced with an independent clean-room del² oracle that genuinely discriminates (buggy diverges O(100)). Never trust a regression test that wasn't seen to fail.
2. **Sibling DomI clone on disk = live verification surface.** `/home/user/DomI` @ `3e46639` let the offline drift fix be verified against a real checkout, not a mock — live hook flipped from dead warn to `✓ pin current (offline sibling clone)`.
3. **Map-first + branch-policy precedence.** Read #48 before write; harness mandated `claude/zealous-noether-efnuo9` but `development` exists w/ rolling PR#150 → worked there per branching.md. Pin stale only on the harness branch, current on `development`.

## What didn't (top 3, evidence)

1. **Caveman plugin `Unknown skill` at bootstrap** → #168 SKILL.md fallback (manual ultra). Known #268; marketplace network-add fails in routine container. Nth confirmed instance.
2. **MCP large-thread/issue-list fetch overflow.** Both #48 comments (86KB) and DomI open-issues (60KB) overflowed the tool result → manual python/jq slicing. Recurring class; paginate from the start.
3. **Drift-gate fix is being hand-ported per repo** (Valence #147, now ADMESH) — divergent copies of identical logic. Filed DomI#286 to canonicalize, but until DomI ships it the redundancy grows with each consumer that hits the dead gate.

## Pain → matrix rows

| Pain | Severity | Freq | Tracking | Saved-min/session |
|---|---|---|---|---|
| caveman warm-load inert (marketplace network-add fail) | low | recurring | DomI#268 / #286-adjacent | 2 |
| MCP large-thread fetch overflow → manual slicing | low | recurring class | habit: paginate | 4 |
| drift-gate fix hand-ported per repo (divergent copies) | medium | recurring | DomI#286 | 6 |

No new `request: skill` — #203 probation respected. DomI#286 filed as `type: bug`, not a skill request.
