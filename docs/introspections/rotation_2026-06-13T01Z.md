<!-- Session introspection + corpus entry. Caveman ultra. -->
---
date: 2026-06-13
session: 2026-06-13T01Z-rotation
repo: domattioli/ADMESH
severity: low
freq: recurring
issues: [152]
wasted_min: 5
wasted_tok: 7000
missing_skill: null
---

# ADMESH rotation 2026-06-13T01Z — maintenance slot

## Summary

Hour-1 rotation → ADMESH. Spec-048 A-tasks still operator-gated (D1/D4/D5); #140 + #133 parked on operator merge → maintenance track. Shipped: (1) DomI pin 69fdeb7→3e46639 via sibling clone (supersedes pin portion of unmerged PR #151); (2) #152 Q3 acquisition probe — younghun-kang/ADMESH still 404, Zenodo/GMD/doi.org all 403 from env, hosted-runner path confirmed as only acquisition route, evidence on #152; (3) suite gate green — 409 pass / 9 skip / 1 xfail, matches spec-048 "A=green" baseline. Claim + checklist on MADMESHing#48. No code edits → no Haiku dispatch (config/docs/probe only).

## What worked (top 3, evidence)

1. Sibling-clone pin path again zero-friction: ancestry check (`merge-base --is-ancestor`) confirmed behind-not-forked before bump.
2. #48 comment-index listing (id/date/first-line) before selective full reads → 64KB thread handled in 2 passes, no overflow retries.
3. Plain `curl` against allowlisted github.com beat ambiguous `git ls-remote` credential prompt for 404-vs-private discrimination on #152 probe.

## What didn't (top 3, evidence)

1. Caveman plugin install failed at bootstrap (marketplace add network fail) → #168 fallback emulation. First instance AFTER DomI PR #274 merged (3e46639) — fix did not cover container-start marketplace network failure. Data point posted to DomI#268.
2. Pin supersession churn: PR #151 (pin→04f5d53) unmerged at slot start; this slot bumps to 3e46639 on a second branch → guaranteed `.domi-pin` merge conflict for operator. Hourly rotation outpaces operator merge cadence.
3. ADMESH still has no `development` branch → harness work branch → draft PR → main remains the A-repo workaround (3rd session running).

## Pain → matrix rows

| Pain | Severity | Freq | Tracking | Saved-min/session |
|---|---|---|---|---|
| caveman bootstrap fallback persists post-#274 merge | low | recurring (1st post-merge) | DomI#268 | 2 |
| unmerged rolling drafts → pin supersession conflicts | medium | recurring (2nd) | — (operator merge cadence) | 5 |
| ADMESH branch policy vs harness branch (no `development`) | medium | recurring (3rd) | branching.md migration gap | 5 |

No new `request: skill` — #203 probation respected.

## Next steps

- [ ] Operator: merge PR #151 then this slot's PR (resolve `.domi-pin` to 3e46639), or close #151 pin-portion as superseded.
- [ ] #152 Q1/Q2 stay blocked on hosted-runner Zenodo pull (#77 pattern); license check first.
- [ ] D1/D4/D5 remain A-side gates; next A slot stays maintenance unless operator decides.
