<!-- Session introspection + corpus entry. Caveman ultra. -->
---
date: 2026-06-13
session: 2026-06-13T13Z-rotation
repo: domattioli/ADMESH
severity: low
freq: recurring
issues: [154, 155]
wasted_min: 5
wasted_tok: 8000
missing_skill: null
---

# ADMESH rotation 2026-06-13T13Z — maintenance slot (hour-13 overhaul rotation)

## Summary

Hour-13 → ADMESH. Spec-048 A-tasks operator-gated (T8/T9→D1, T12→D4, P6→D5); #155 inpaint Laplacian already shipped 07Z (`8f7e005`) → maintenance track. Claimed + shipped **A#154** (operator directive: large-domain benchmark standard WNAT→ENPAC 2003): extracted portable `enpac_boundary.json` (240 KB, 10,365-node ring) from the 29 MB `.14` (stays in Valence/HF), added `bench_enpac.py` standard gate, retargeted `compare_versions.py` default + domain-stem PNG name, kept WNAT as smoke, doc'd the switch. `e3d7391` → `development` → rolling PR #150. Code → Haiku subagent; orchestrator extracted boundary + measured SDF/triangulate cost + re-ran bench before commit. Pin in-sync. Claim+checklist on #48.

## What worked (top 3, evidence)

1. Map-first (#48 read before write) → caught that #155 (top of queue) was already fixed 07Z (`8f7e005`, git blame) → re-picked #154, zero duplicate work.
2. Verify-don't-assume on the target: #155's described buggy line (`np.concatenate`) did NOT match the live file (already `column_stack(...).ravel()`) → surfaced "already shipped" instead of re-fixing a fixed bug.
3. Measured SDF cost (warm 0.012 ms/pt) + coarse-triangulate timing BEFORE sizing the Haiku bench spec → bench completes ~12 s, no hang; Haiku produced correct code first pass.

## What didn't (top 3, evidence)

1. #48 comment fetch = 99 KB → MCP result token-cap overflow → manual python/jq slicing of the dumped file. Recurring class (also 06-12T19Z entry). ~4 min.
2. Caveman plugin not loaded at bootstrap → `Unknown skill: caveman:caveman` → #168 fallback line emitted. Known #268; warm-load fix (`ensure_caveman.sh`, 12Z hub) not yet on `main` → 3rd confirmed post-fix instance.
3. `bench_wnat.py` reads a `{vertices,boundaries,bathymetry}` JSON schema but the committed `wnat_onur_boundary.json` is `{name,bbox,rings}` → bench_wnat KeyErrors on its own fixture. Latent dead/divergent benchmark code; on-theme for #48 redundancy. Filed ADMESH#158.

## Pain → matrix rows

| Pain | Severity | Freq | Tracking | Saved-min/session |
|---|---|---|---|---|
| MCP large-thread fetch overflow → manual slicing | low | recurring class | — (habit: paginate `perPage` + jq from start) | 4 |
| caveman warm-load not on `main` yet (#268 AC1) | low | recurring (3rd) | DomI#268 | 2 |
| bench_wnat.py schema vs committed fixture mismatch | low | one-off (found) | ADMESH#158 | 3 |

Resolved since last A entry: "ADMESH has no `development` branch" (06-12T19Z pain) — `development` now exists + rolling PR #150; shipped there directly, no harness-branch ambiguity this slot.

No new `request: skill` — #203 probation respected.
